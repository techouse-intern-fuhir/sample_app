<%# form_withはUser.new_record?で渡ってきた@userがデータベースに存在するか否かを確認してpost処理にするか、patch処理にするか決めている %>
<%# @userのmodelの種類からルートやparamsの構造などを決定している→シュガーシンタックス %>
<%# →form_withの(model:)での役割：　リダイレクト先の指定、name属性のハッシュによるグループ化、インスタンス変数によるpost,patchの指定,
                                validation結果のhtmlへの伝播 %>
<%= form_with(model: @user) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.label :email %>
  <%= f.email_field :email, class: 'form-control' %>

  <%= f.label :password %>
  <%= f.password_field :password, class: 'form-control' %>

  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
